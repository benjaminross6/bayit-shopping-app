services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bayit
      POSTGRES_PASSWORD: bayit
      POSTGRES_DB: bayit
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bayit"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
